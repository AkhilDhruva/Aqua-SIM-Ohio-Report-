# Pinned to the originating host's versions. Changing these risks failing the
# cross-host equivalence gate, which is the point of the gate.
numpy==2.4.6
scipy==1.17.1
rasterio==1.4.4
fiona==1.10.1
shapely==2.1.2
