{
  "updated_utc": "2026-08-23T07:06:17.451677+00:00",
  "purpose": "Engineering failures and measured limitations are part of the reproducibility record, not noise to be deleted.",
  "entries": [
    {
      "id": "EF-1",
      "failure": "launcher process guard used a full-command-line match (pgrep -f), which matched the bash wrapper whose command text CONTAINED the script \u2014 false-positive 'run already active'",
      "effect": "the watchdog would have refused every legitimate restart; with the host recycling every ~3 h, no corridor run would ever have advanced again",
      "detection": "manual audit during a watchdog tick (launcher reported a run active while ps showed no python3 solver)",
      "correction": "guard now matches only real python3 processes (ps comm=='python3' AND args ~ run_corridor.py)",
      "affected_scientific_results": "none identified"
    },
    {
      "id": "EF-2",
      "failure": "corridor_analysis.py labelled a partial run FINAL because a stale meta.json from a short smoke test satisfied the completeness check",
      "effect": "a partial probe series could have been reported as a final result",
      "detection": "self-review of the first analysis output",
      "correction": "completeness now requires the probe series to actually reach the run's target window",
      "affected_scientific_results": "none \u2014 caught before any partial output was used in a claim"
    },
    {
      "id": "EF-3",
      "failure": "probe scheduler deadline (_next) was restored as time+interval instead of its saved value",
      "effect": "a restart could silently skip one probe sample and shift the probe grid; probe continuity LOOKED fine while the schedule drifted",
      "detection": "state audit prompted by external review before the wet-phase equivalence test",
      "correction": "_next is now saved in and restored from the checkpoint; the wet-phase A/B test compares every probe record bitwise",
      "affected_scientific_results": "none for completed analyses (all published probe series came from runs without a mid-interval restart of this kind)"
    },
    {
      "id": "ML-1",
      "measured_limitation": "the solver executes on ONE core (~100% CPU measured via /proc jiffies: 999/10 s), not four \u2014 elementwise NumPy kernels do not use BLAS threading",
      "consequence": "sequential scheduling buys restart robustness and memory headroom, not compute acceleration; wall-time projections must assume single-core throughput",
      "decision": "solver is NOT being rewritten or parallelized mid-experiment; the limitation is recorded instead"
    },
    {
      "id": "ML-2",
      "measured_limitation": "restart start-up cost dominated short process lifetimes: rebuilding nest terrain, roughness, culverts, probe geometry and the parent-derived stage BC took ~50 s, while the recycling host was giving processes only 2-5 minutes of life",
      "measurement": "rate analysis of checkpoint log lines showed the SOLVER is not the bottleneck (~2 wall-min per model-hour in the dry phase); the run was losing most of each lifetime to deterministic setup",
      "correction": "setup is now cached to a 1 MB npz and reloaded in 0.2 s (250x faster start-up). The cache is a pure function of frozen inputs, and tests/solver_equivalence/test_setup_cache.py proves the reconstruction is array-identical to a fresh build (z, roughness, mask, rain index map, culverts, probe groups, transform, CRS, stage BC).",
      "affected_scientific_results": "none \u2014 verified identical before any cached run output was used"
    },
    {
      "id": "EF-4",
      "failure": "CHECKPOINT LIVELOCK \u2014 the wall-clock checkpoint interval (420 s) was longer than the host's shortest process lifetime (as little as 1-2 min), so every process died before its first checkpoint and the run resumed from the same model hour indefinitely",
      "observation": "buckeye_c sat at model hour 6.50 with 390 probe samples for 37 minutes across three relaunches, each reporting a successful RESUME. Every individual component was working; the system as a whole made zero progress.",
      "why_it_hid": "resume was verified as CORRECT (bit-identical) and start-up was verified as FAST (0.2 s). Both were true. The failure was in the interaction between checkpoint cadence and host lifetime, which neither component test could expose.",
      "correction": "CKPT_WALL_S 420 s -> 60 s. Measured cost: 0.49 s per checkpoint (6.4 MB), i.e. 0.8% overhead. Progress is now monotonic.",
      "generalizable_lesson": "a checkpoint interval must be shorter than the host's SHORTEST observed lifetime, not merely shorter than its average. Averages hide livelock.",
      "affected_scientific_results": "none \u2014 no result was produced during the stalled period; the cost was wall-clock time only"
    },
    {
      "id": "EF-5",
      "failure": "MISDIAGNOSIS \u2014 concurrent corridor runs were abandoned for sequential execution on the belief that three solves on four cores 'were not converging before the next recycle'",
      "actual_cause": "the runs were not converging because of the EF-4 checkpoint livelock (checkpoint interval longer than host lifetime), which applied equally to sequential execution. Core contention was not the cause.",
      "effect": "three of four cores sat idle while a single-threaded solver ran; total throughput was roughly one third of what the host could deliver",
      "detection": "a direct capacity check (nproc, load average, per-process CPU) prompted by the question of whether the work could be parallelised",
      "correction": "parallel execution restored, capped at nproc-1 to leave a core for the OS and analysis tooling; priority order retained so the highest-value run starts first after a recycle",
      "affected_scientific_results": "none \u2014 scheduling changes cannot alter a deterministic solve; the cost was wall-clock time only",
      "execution_environment_note": "compute runs on a remote 4-core Intel Xeon @2.10GHz VM with 15 GB RAM and NO GPU (no NVIDIA device nodes, no CUDA). Local operator hardware is not involved in the computation. Any GPU or many-core acceleration is therefore a change of execution environment, not a code change, and is deliberately deferred until after this experiment."
    },
    {
      "id": "ML-3",
      "measured_limitation": "buckeye_c solves 6-8x slower than the other corridors (1.83 model-hours per wall-hour vs 10.8 franklinton / 15.1 pataskala) despite fewer cells than pataskala and shallower water than franklinton",
      "diagnosis": "its global CFL timestep is VELOCITY-limited at 21.35 m/s while the others are wave-limited at 3-4 m/s. The 21.35 m/s occurs on exactly 5 cells of the SOUTHERN DOMAIN EDGE, where the OPEN boundary places a ghost dry cell at the live cell's own bed: deep channel water therefore sees a free drop of its full depth over one cell width and exits at high speed. dt = cfl*dx/(v+sqrt(g*h)) is global, so five boundary cells throttle 0.68M cells.",
      "ruled_out": [
        "thin-film velocity spike \u2014 the fastest face carries 882 mm of water and the value is identical at min_depth 1e-3, 1e-4 and 1e-5",
        "stage-BC jet \u2014 max velocity anywhere on the BC ring is 1.39 m/s, and zero cells above 5 m/s lie within two cells of the ring",
        "interior instability \u2014 interior maximum is 3.47 m/s with NO cells above 5 m/s anywhere in the domain"
      ],
      "physics_impact": "none identified in the interior. The affected cells are the outflow boundary itself; the corridor probes are far inside. The free-outfall OPEN boundary is the pre-registered configuration and behaves as designed.",
      "decision": "NOT changed. Altering the boundary treatment or the CFL velocity mid-experiment would change the frozen configuration and invalidate the comparison. The cost is wall-clock time only, and it is recorded rather than optimised away.",
      "for_future_work": "a domain-edge outflow treatment (e.g. normal-depth or critical-depth outflow instead of free drop to bed), or excluding boundary faces from the global CFL reduction, would remove this throttle. Both are engine changes and belong to the aqua-sim backlog, not to this study."
    },
    {
      "id": "ML-4",
      "measured_limitation": "the execution container is torn down between interactive turns, so the solver only advances WHILE A TURN IS ACTIVE. Background processes do not accumulate compute in the gaps.",
      "measurement": "summed process wall time from the checkpoint logs against elapsed clock time since the corridor runs began: buckeye 62.2 min solved / 492 min elapsed = 12.6% duty cycle; franklinton 9.5%; pataskala 9.8%. Cross-check on a single interval: 72 min of clock time between watchdog ticks yielded 0.14 model-hours for buckeye, which at its measured 1.83 model-h per wall-hour is 4.6 min of compute \u2014 approximately the length of the turn itself.",
      "why_it_hid": "every individual mechanism worked. Checkpoint/resume was proven bit-identical, start-up was cached to 0.2 s, the livelock was fixed, and parallelism was restored. Each watchdog tick honestly reported progress. The runs were simply frozen between ticks, and no per-component test could show that.",
      "consequence": "polling on an hourly watchdog cannot finish these runs. Remaining compute at measured rates: franklinton ~57 min, pataskala ~37 min, buckeye ~6.8 hours (buckeye inflated ~5x by the ML-3 boundary CFL throttle). At a few minutes of compute per tick, buckeye alone would need on the order of 100 ticks.",
      "affected_scientific_results": "none \u2014 no result is altered, only deferred. The Case B determination rests on surveyed geometry and observed gauge timing, neither of which depends on these runs.",
      "implication_for_future_work": "sustained simulation needs an execution environment that persists between interactions. This is the concrete justification for moving compute off this platform before any regional-scale benchmark."
    }
  ]
}